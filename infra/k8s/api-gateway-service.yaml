apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: devops-ai
  labels:
    app: api-gateway
    feature: k8s-terraform
    week: "2"
spec:
  type: ClusterIP
  selector:
    app: api-gateway
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
